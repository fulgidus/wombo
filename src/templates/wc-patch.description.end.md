Deployed by wombo-combo as an autonomous agent in an isolated git worktree.
Implements tasks from {{tasksFile}} — a recursive tree of identical atomic
jobs — with zero human interaction. Launched headlessly by `woco launch`.
